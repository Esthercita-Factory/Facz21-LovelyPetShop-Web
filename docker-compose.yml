services:
  lovelypetshop-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lovelypetshop_app
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
    restart: unless-stopped
